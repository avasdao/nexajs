export function generateSigningSerializationNEXA(_params) {
    const {
        correspondingOutput,
        coveredBytecode,
        forkId,
        locktime,
        outpointIndex,
        outpointTransactionHash,
        outputValue,
        sequenceNumber,
        sha256,
        signingSerializationType,
        transactionOutpoints,
        transactionOutputs,
        transactionSequenceNumbers,
        version,
    } = _params

    return 'hi there!'
}
